apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      volumes:
      - name: model-cache
        persistentVolumeClaim:
          claimName: gpt4all-cache
      initContainers:
      - name: download-model
        image: curlimages/curl:latest
        command:
        - sh
        - -c
        - |
          mkdir -p /home/app/.cache/gpt4all
          # Remove old 8B model if it exists to free up space
          if [ -f /home/app/.cache/gpt4all/Meta-Llama-3-8B-Instruct.Q4_0.gguf ]; then
            echo "Removing old 8B model..."
            rm /home/app/.cache/gpt4all/Meta-Llama-3-8B-Instruct.Q4_0.gguf
          fi
          # Download 1B model if not present
          if [ ! -f /home/app/.cache/gpt4all/Llama-3.2-1B-Instruct-Q4_K_M.gguf ]; then
            echo "Downloading Llama 3.2 1B model from HuggingFace..."
            curl -L -o /home/app/.cache/gpt4all/Llama-3.2-1B-Instruct-Q4_K_M.gguf \
              https://huggingface.co/bartowski/Llama-3.2-1B-Instruct-GGUF/resolve/main/Llama-3.2-1B-Instruct-Q4_K_M.gguf
          else
            echo "Model already exists, skipping download"
          fi
        volumeMounts:
        - name: model-cache
          mountPath: /home/app/.cache
      containers:
      - name: backend
        env:
          - name: HOME
            value: /home/app
          - name: XDG_CACHE_HOME
            value: /home/app/.cache
          - name: NODE_OPTIONS
            value: "--dns-result-order=ipv4first"
          - name: GITHUB_CLIENT_ID
            valueFrom:
              secretKeyRef:
                name: backend-secrets
                key: GITHUB_CLIENT_ID
          - name: GITHUB_CLIENT_SECRET
            valueFrom:
              secretKeyRef:
                name: backend-secrets
                key: GITHUB_CLIENT_SECRET
          - name: JWT_SECRET
            valueFrom:
              secretKeyRef:
                name: backend-secrets
                key: JWT_SECRET
          - name: FRONTEND_URL
            valueFrom:
              configMapKeyRef:
                name: backend-config
                key: FRONTEND_URL
          - name: RATE_LIMIT_PER_MINUTE
            valueFrom:
              configMapKeyRef:
                name: backend-config
                key: RATE_LIMIT_PER_MINUTE
          - name: RATE_LIMIT_PER_DAY
            valueFrom:
              configMapKeyRef:
                name: backend-config
                key: RATE_LIMIT_PER_DAY
          - name: JWT_EXPIRATION_HOURS
            valueFrom:
              configMapKeyRef:
                name: backend-config
                key: JWT_EXPIRATION_HOURS
          - name: LLM_MODEL_NAME
            valueFrom:
              configMapKeyRef:
                name: backend-config
                key: LLM_MODEL_NAME
          - name: JWT_ALGORITHM
            valueFrom:
              configMapKeyRef:
                name: backend-config
                key: JWT_ALGORITHM
        volumeMounts:
          - name: model-cache
            mountPath: /home/app/.cache
        image: mini-llm-backend
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
        startupProbe:
          httpGet:
            path: /health
            port: http
          periodSeconds: 10
          failureThreshold: 30
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            memory: "2Gi"
            cpu: "250m"
          limits:
            memory: "3Gi"
            cpu: "1000m"
